<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.addie.maxfocus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.AppDialogIntentService"
            android:exported="false" />

        <activity android:name=".ui.DialogActivity" android:theme="@style/Theme.Transparent"></activity>

        <receiver
            android:name=".receiver.AppDialogBroadcastReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.addie.maxfocus.service.action.APP_DIALOG" />
            </intent-filter>
        </receiver>
    </application>

</manifest>